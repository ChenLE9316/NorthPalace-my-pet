# Temporary transport chunks

This directory exists only for a one-shot verified asset import. The importer reconstructs the WebP, verifies SHA-256 and byte length, then removes this directory and itself in the resulting commit.
